/**
 * WebSocketChannel.ts
 * Using WebSocket for communication between the extension background and the remote host.
 * Using the WebSocket object, we will establish a Long-lived connection.
 * messaging flow: remote host → background → remote host
 * author: zhang jie
 */
class WebSocketChannel {
}