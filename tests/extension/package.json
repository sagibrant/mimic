{
  "name": "extension",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build::clean": "rimraf dist",
    "build::assets": "tsc && node dist/tests/extension/build-assets.js",
    "build": "run-s build::clean build::assets --"
  },
  "author": "Sagi",
  "license": "Apache-2.0",
  "description": "generate the asset files for extension testing",
  "devDependencies": {
    "@types/node": "^24.6.1",
    "npm-run-all": "^4.1.5",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.2"
  }
}
